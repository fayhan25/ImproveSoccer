BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Post] ADD [CreatedDate] DATETIME2 NOT NULL CONSTRAINT [Post_CreatedDate_df] DEFAULT CURRENT_TIMESTAMP,
[messages] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
